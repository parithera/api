/**
 * Enum representing sort directions for data sorting.
 */
export enum SortDirection {
    /**
     * Sort direction in descending order (highest value first).
     */
    DESC = 'DESC',

    /**
     * Sort direction in ascending order (lowest value first).
     */
    ASC = 'ASC'
}